# AI Usage Notes

- I used an AI assistant to scaffold the project, write boilerplate (Vite, Tailwind, Vitest, Playwright), and implement
  the mocked API with retries and AbortController.
- I asked AI to help with accessible patterns (aria-live, focus-visible) and summarize design decisions.
- Tests were bootstrapped with AI-generated examples (RTL render smoke test, Playwright smoke).
- All code was reviewed locally and adapted to TypeScript strict mode.

Evidence ideas:

- Screenshots of chat threads and CI runs in `evidence/`.
